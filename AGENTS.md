這裡為您將所有討論的精華（商業邏輯、UI UX 風格、工具箱架構、LINE OA 接軌），整理成一份標準的**專案開發啟動宣告書**。

這份文件的結構包含 **【專案目的】**、**【專案背景】** 與 **【操作守則】**。未來您可以直接將這整段內容（連同下方的 Markdown 格式）原封不動地貼給 **Cursor** 或任何 AI 編輯器，作為專案的「大腦與根本方針」，讓 AI 絕對不會偏離軌道。

---

# 📋 <百果旅遊工具盒> 專案開發啟動宣告書

## 一、 專案目的 (Project Purpose)

本專案旨在建置一個高質感、輕量化的多功能數位工具箱平台——**「百果旅遊工具盒 (Baiguo Travel Toolbox)」**。

* **近期目標（首發旗艦工具）：** 開發 **旅遊產品「客製化代訂與價差共享」工具**。允許消費者自主貼上第三方旅遊平台（如 Klook、KKday、Agoda 或各大旅行社）的商品連結與聯絡方式（LINE ID），由系統儲存並觸發 Webhook 通知，引導消費者加入 LINE 官方帳號由真人團隊接手 B2B 同業查價與轉單分潤，實現精實創業（MVP）的商業閉環。
* **遠期目標：** 採用高擴充性的單頁應用（SPA）結構，未來可無縫增加其他獨立的旅遊輔助小工具（如：旅遊記帳器、行程抽獎輪盤等），打造如「雷蒙三十工具集」般的極簡功能型工具大廳。

## 二、 專案背景 (Project Background)

1. **市場痛點與機會：** 旅遊市場存在嚴重的資訊不對稱。一般消費者只能接觸到 B2C 的市面零售價，而同業（同行人工或 B2B 後台）則擁有更具優勢的底價與差價空間。本工具不涉入繁重的電商上架流程，而是讓「客人自己找商品」，平台純粹扮演「特殊管道代購與利潤共享」的中介者，幫客人省錢的同時賺取分潤。
2. **技術選型背景：** 為追求極致的載入速度、極低的維護成本，並確保未來多個工具間互不干擾，本專案**拒絕使用臃腫的 WordPress**。改採純 AI 開發最擅長的輕量化架構：前端使用 `HTML5` + `Tailwind CSS` + `Vanilla JavaScript`，後端使用 `Node.js` 搭配輕量 `SQLite` 資料庫，並預計免費部署於 `Zeabur` 平台，結合 `Cloudflare` 進行網域管理與資安防護。

## 三、 操作守則 (Operating Principles & Guide)

當 AI（如 Cursor）在讀取、修改、擴充本專案時，**必須嚴格遵守以下操作規範**：

### 1. UI/UX 視覺設計守則（核心看齊：雷蒙三十風格）

* **極簡高質感：** 背景必須保持乾淨清爽（淺灰 `#F9FAFB` 或純白），文字使用深色系以維持良好對比與閱讀舒適度。
* **卡片式佈局：** 首頁大廳必須使用 Grid 網格佈局，各別工具以「獨立卡片」形式呈現。卡片必須包含細緻灰色邊框與大圓角（`rounded-2xl` 以上）。
* **動態特效：** 網頁物件（尤其是工具卡片與主按鈕）必須具備優雅的滑鼠懸停效果（如：`hover:shadow-lg`、`hover:-translate-y-1`），且必須加上平滑的過渡動畫（`transition-all duration-300`），拒絕生硬的切換。

### 2. 程式碼架構與擴充守則

* **零臃腫原則：** 優先使用原生 JavaScript（Vanilla JS）控制前端邏輯與 Modal 彈窗，避免引入不必要的第三方大型套件。
* **目錄獨立性：** 保持專案結構乾淨。首頁 `index.html` 負責工具大廳導覽。首發旅遊工具應具備獨立的邏輯區塊（或資料夾），確保未來增加「工具 B、工具 C」時，程式碼可以各自獨立、不互相打架。
* **資料庫備援：** 每次表單送出時，後端必須確實將資料存入 `SQLite` 以利未來對帳，並預留欄位以利中後期無縫升級至 PostgreSQL。

### 3. 客戶導流與 LINE OA 接軌守則

* **禁止死胡同：** 使用者點擊「獲取同業折扣價」主按鈕後，網頁不可只是重新整理。必須跳出一個高質感的成功提示彈窗（Modal）。
* **強制引導：** 彈窗內必須清晰說明下一步（*“專員正透過 B2B 後台爭取差價...”*），並必須提供一個顯眼的**綠色 LINE 官方帳號導流按鈕**。
* **API 友善：** 後端程式碼必須預留 **LINE Messaging API** 的 Webhook 接腳與參數傳遞邏輯，以便未來隨時將「真人客服」升級為「AI 智能客服接手」的自動化流程。

---

